import React, { Component } from 'react';
import {
  View
} from 'react-native';

export default () => {
  return (
    <View
      style={{
        borderBottomColor: 'rgba(0, 0, 0, 0.1)',
        borderBottomWidth: 1
      }}
    />
  );
}
