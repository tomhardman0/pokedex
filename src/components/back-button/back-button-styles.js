import {
  StyleSheet
} from 'react-native';

export default styles = StyleSheet.create({
  button: {
    position: 'absolute',
    top: 30,
    left: 20,
    zIndex: 200
  },
  icon: {
    height: 40,
    width: 40
  }
});
